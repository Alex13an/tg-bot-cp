{
  "name": "cp-tg-bot",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run server.ts",
    "build": "rm -rf ./dist & bun build ./server.ts --outdir dist --target=bun"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "express": "^5.1.0",
    "grammy": "^1.36.1"
  }
}
